---
interface Props {
	id?: string;
	class?: string;
}

const { id, class: className } = Astro.props;
---

<div id={id} class:list={["mx-auto max-w-[92%] sm:max-w-[85%]", className]}>
  <slot />
</div>